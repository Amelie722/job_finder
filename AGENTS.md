[RULES-OK]
# Agent Instructions

## Project Structure

- `index.html`은 화면, 스타일, 동작을 모두 포함하는 단일 정적 페이지다.
- 패키지 매니저, 빌드 도구, 테스트 러너, 로컬 서버는 현재 없다.
- 명시적 요청 없이 프레임워크, 의존성, 서버 파일을 추가하지 않는다.

## Verification

| Task | Command or check |
| --- | --- |
| 변경 범위 확인 | `git diff --check` |
| 저장소 상태 확인 | `git status --short` |
| 화면 확인 | `index.html`을 브라우저에서 열어 입력·검색·카드 표시를 확인 |

## Product Rules

- 사용자는 학과, 희망 지역, 졸업 시기, 공고 검색어를 입력한다.
- 희망 지역을 선택하고 `검색`을 누르면 해당 지역 공고만 표시한다.
- 선택 지역이 없으면 모든 지역 공고를 표시한다.
- 공고 데이터의 근무 지역이 대전·세종·충남이면 `지역 우선 규칙`을 적용한다.
- 그 외 지역은 재택근무 가능 공고에만 `지역 외 재택 가능 규칙`을 적용한다.
- 검색어가 공고 제목, 직무명, 담당 업무 중 하나에 있으면 `검색 키워드 일치 규칙`을 적용한다.
- 적용 규칙 수가 많은 공고를 위에 표시하고, 카드마다 `추천 이유` 한 줄로 규칙 이름을 표시한다.
- 인턴·신입 구분, 지원 자격, 마감일은 추천 점수와 제외 조건에 사용하지 않는다.

## UI Requirements

- 공고 카드에는 기관명, 공고 제목, 근무 지역, 마감일, 원문 링크를 표시한다.
- 근무 지역과 재택근무 가능 여부를 카드에서 가장 눈에 띄게 표시한다.
- 지역 외 재택 가능 공고에는 `지역 외·재택 가능` 배지를 표시한다.
- 검색어 일치 위치에 따라 `제목 일치`, `직무명 일치`, `담당 업무 일치` 배지를 표시한다.
- `관심 없음`을 누른 공고는 원래 위치에서 흐리게 표시한다.
- 공고를 누르면 해당 채용 페이지로 연결한다.

## Exclusions

- `관심 없음` 공고를 숨기거나 목록 맨 아래로 내리지 않는다.
- 공고를 밀어서 숨기거나 여러 공고를 한꺼번에 숨기지 않는다.
- 학과 또는 학년을 추천 규칙에 사용하지 않는다.
- API 키, 담당자 이름, 전화번호, 이메일을 코드나 문서에 저장하지 않는다.
