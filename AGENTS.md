## 만드는 것

- 충남대학교 학생이 인턴 또는 신입 공고를 찾는 화면 하나
- 사용자가 입력한 검색 키워드와 근무 지역을 기준으로 지원할 공고를 찾는 화면
- 사용자의 학과: (모름)
- 사용자의 학년: (모름)

## 화면

- 검색 키워드 입력창
- 공고 제목
- 직무명
- 담당 업무
- 근무 지역
- 재택근무 가능 여부
- 인턴·신입 구분
- 공고 카드에서는 근무 지역과 재택근무 가능 여부를 가장 눈에 띄게 표시
- 대전·세종·충남 외 지역의 재택 가능 공고에는 `지역 외·재택 가능` 배지 표시
- 검색 키워드가 일치한 위치에 따라 `제목 일치`, `직무명 일치`, `담당 업무 일치` 배지 표시
- 각 공고에 `관심 없음` 버튼 표시
- 회사·기관명 표시 여부: (모름)
- 지원 자격 표시 여부: (모름)
- 마감일 표시 여부: (모름)

## 추천 규칙

- 공고 데이터의 근무 지역이 대전·세종·충남 중 하나이면 지원 대상으로 본다.
- 공고 데이터의 근무 지역이 대전·세종·충남 외 지역이면 재택근무가 가능한 경우에만 지원 대상으로 본다.
- 사용자가 입력한 검색 키워드가 공고 제목, 직무명, 담당 업무 중 한 곳 이상에 포함되어야 지원 대상으로 본다.
- 지원 대상 공고는 대전, 세종, 충남 순서로 위에 올린다.
- 대전·세종·충남 공고와 지역 외 재택 가능 공고 사이의 순서: (모름)
- 인턴·신입 구분은 추천 및 제외 조건에 사용하지 않는다.
- 지원 자격은 추천 및 제외 조건에 사용하지 않는다.
- 마감일까지 남은 기간은 추천 및 제외 조건에 사용하지 않는다.
- `관심 없음`으로 표시한 공고는 순서를 바꾸지 않고 원래 위치에 둔다.

## 평가 기준

- 검색 키워드를 입력하고 검색을 누르면, 키워드가 공고 제목·직무명·담당 업무 중 한 곳 이상에 있는 공고가 보인다.
- 지역 조건을 누르면, 대전·세종·충남 공고가 대전, 세종, 충남 순서로 보인다.
- 대전·세종·충남 외 지역 공고는 공고 데이터에 재택근무 가능으로 표시된 경우에만 보인다.
- 공고 카드가 보이면 근무 지역과 재택근무 가능 여부가 가장 눈에 띄게 보인다.
- 지역 외 재택 가능 공고가 보이면 `지역 외·재택 가능` 배지가 보인다.
- 검색 결과가 보이면 각 공고에 키워드 일치 위치에 따라 `제목 일치`, `직무명 일치`, `담당 업무 일치` 배지가 보인다.
- `관심 없음`을 누르면 해당 공고가 흐리게 보인다.
- `관심 없음`을 누른 공고도 원래 위치에 그대로 보인다.
- 공고를 눌렀을 때 보이는 상세 내용: (모름)

## 이번에 안 만드는 것

- `관심 없음` 공고를 목록에서 즉시 숨기는 기능
- `관심 없음` 공고를 목록 맨 아래로 내리는 기능
- 공고를 밀어서 숨기는 기능
- 여러 공고를 선택해 한꺼번에 숨기는 기능
- 학과 또는 학년을 이용한 추천: (모름)
- 그 밖의 제외 범위: (모름)
